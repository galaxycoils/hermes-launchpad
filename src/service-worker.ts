/// <reference lib="webworker" />

// This is a minimal service worker entry. The Vite PWA plugin generates the
// actual SW during build via Workbox; this file is kept for any custom
// registration or fetch logic that may be added later.
//
// If you need to add custom fetch handlers or extend the generated SW,
// uncomment below and adjust as needed:
//
// self.addEventListener('fetch', (event) => {
//   // Custom fetch handling
// })
