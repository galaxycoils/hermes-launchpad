Hermes Launchpad is a Solana devnet fair-launch memecoin app: on-chain bonding-curve program, React UI with wallet, Cloudflare Worker API that indexes verified create/trade txs into D1 so the feed shows real onchainMints. Do not use the removed register path; use POST /api/tokens/index and POST /api/trades/index. Keep verification and keyed SOLANA_RPC. Scope is devnet preview, not mainnet launch.
